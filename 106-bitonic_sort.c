#include "sort.h"
/**
 * bitonic_sort - sort
 * @array: Array
 * @size: size
 * Return: void
 */
void bitonic_sort(int *array, size_t size)
{
	int a = 1, b = 2;

	if (a == 2 || b == 1 || !array || size < 2)
		return;	
}
